Available Maven profiles:

- assembly: 	builds a zip file containing Tomcat + collect-webapp among its webapps
- autoupdater: 	builds the auto-udpater executable file
- installer:	builds the installers
- updater: 		builds the updaters (used by the autoupdater)
- publish:		publish the udpate using the specified parameters (
 
Release a new version:

mvn release:prepare
mvn release:perform

(All profiles are active during the release).

See http://www.openforis.org/wiki/index.php/Open_Foris_Collect_Development_Environment for detailed instructions/
