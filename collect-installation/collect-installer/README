Required Maven parameters:

collect.update.url = Url of AutoUpdate tool update.xml file 

To build the installer, use the "installer" profile:

mnv build -Pinstaller

or specify the "release" property (value does not matter):

mvn build -Drelease

The installer will be generated in the "target" folder

(see BitRock InstallBuilder AutoUpdate Tool for reference http://installbuilder.bitrock.com/docs/installbuilder-userguide/ar01s23.html) 